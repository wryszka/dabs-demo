resources:
  jobs:
    dabs_demo_job:
      name: dabs_demo_job

      tasks:
        - task_key: hello_task
          spark_python_task:
            python_file: ../src/hello.py
          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
